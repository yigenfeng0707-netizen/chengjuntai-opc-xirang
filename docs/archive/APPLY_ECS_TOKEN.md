# 内部申请稿：天翼云试用 ECS + 息壤/星辰 TokenHub Token

> 用途：中国电信员工内部审批 / OA / 邮件直接粘贴。  
> **勿写入** API Key、服务器密码、公网 IP、证件号等敏感信息。  
> 规格依据：`DEPLOY天翼云.md` §3、`docs/CTYUN_TRIAL.md`。  
> 推荐优先使用 **稿件 A（合并申请）**；需分单走流程时用 **稿件 B**。

---

## 申请人信息（粘贴前填写）

| 项 | 填写 |
|----|------|
| 姓名 | [姓名] |
| 工号 | [工号] |
| 部门 | [部门] |
| 手机 | [手机] |
| 邮箱 | [邮箱] |

---

## 稿件 A：合并申请（推荐）

**主题：** 申请天翼云试用 ECS 及息壤/星辰 TokenHub Token —— 用于 2026「息壤杯」参赛项目「成军台」公网 Demo 与主模型推理

尊敬的审批领导 / 相关同事：

现就参加 **2026「息壤杯」全国人工智能 OPC 创新大赛** 所需云主机与模型 Token，一并申请如下资源，请予支持。

### 一、申请事由 / 赛事背景

本人以 OPC（一人公司）身份参赛，赛道为 **惠民产品创新 · AI+自选开放场景**。赛事要求提供可访问的公网 Demo，并以中国电信息壤 / 星辰 TokenHub 作为正式演示主模型链路。当前工程与本地演示已就绪，缺 **公网部署主机** 与 **竞赛主链路 Token**，恳请开通试用 ECS 并协调发放 / 开通 Token，以便完成预赛材料与评委远程验收。

### 二、项目简介

**一句话：** 成军台（OPC OS on 息壤）——输入目标即可调度 AI 员工矩阵，协同完成调研、内容、问数、运营与复盘，服务一人公司与小微经营者。

补充说明：

1. 作品定位为「一人成军」作战系统：目标驱动战役、多角色协同、人审卡点、一键导出 Word 成军周报。  
2. 技术栈：Python / FastAPI / 多 Agent / NL2SQL MCP；可部署于天翼云；主模型对接息壤 / 星辰 TokenHub。  
3. 代码仓库（公开）：https://github.com/yigenfeng0707-netizen/chengjuntai-opc-xirang  
4. 本地已具备：过渡模型（interim）联调能力、真实政采标讯约 **382** 条入库、评委「60 秒体验」零讲解路径（样例战役 → Word 周报 → 智能问数）。  
5. 报名入口参考：惠民产品创新（AI+自选开放场景）；作品名「成军台」。

### 三、用途说明

| 资源 | 用途 |
|------|------|
| 天翼云试用 ECS | 部署参赛 **公网 Demo**，供评委 / 主办方远程访问登录页与看板；Nginx 对外提供 HTTP/HTTPS；支撑预赛提交「Demo 地址」字段。 |
| 息壤 / 星辰 TokenHub Token | 作为竞赛 **primary LLM**，支撑多 Agent 战役推理、内容生成与正式演示；到手后注入环境变量并切换模型徽章，与 PPT/视频口径一致。 |

两项互不阻塞：可先上公网结构 Demo，Token 到手后切换主模型。

### 四、资源规格建议

**1）天翼云 ECS（试用，与控制台默认试用档对齐）**

| 项 | 建议 |
|----|------|
| 规格 | 4 vCPU / 8 GB 内存（试用默认档即可） |
| 系统盘 | 60 GB |
| 带宽 | 3 Mbps（试用）；若后续转按量，公测阶段可酌情 5 Mbps |
| 操作系统 | Ubuntu 22.04 |
| 对外端口（安全组） | TCP **80**、**443**（公网访问）；TCP **22**（SSH，建议限源 IP） |
| 应用端口说明 | Web 服务监听 **8090**，经 Nginx 反代，**原则上不对公网直开**；NL2SQL/MCP 等内网端口同理不对外开放 |
| 部署方式 | 仓库已备 `deploy_to_ctyun.sh` 与部署文档，开通后即可上传部署 |

试用入口（自行操作时参考）：https://www.ctyun.cn/act/trial/central  

**2）息壤 / 星辰 TokenHub Token**

| 项 | 说明 |
|----|------|
| 用途 | 预赛及后续赛程公网 Demo / 本地联调的 **主模型 API Key**（OpenAI 兼容网关） |
| 需要信息 | API Key；推荐 `api_base`；可用 `model` 名称；额度与有效期 |
| 注入方式 | 仅环境变量（如 `XIRANG_API_KEY` / `TOKENHUB_API_KEY`）或服务器本地配置，**不入库、不入 Git** |
| 调用量级（预估） | **演示 + 开发联调** 量级：日常联调与评委演示为主，预计日调用约 **数百～一两千次** 请求量级（短文本/战役编排为主，非训练、非批量爬取）；若额度有档位，申请「演示开发」档即可，无需超大配额 |

### 五、使用期限

建议授权使用至 **赛事复赛 / 决赛结束，或赛事正式截止日后另加 7～14 天缓冲**（用于材料补交、答辩复盘与资源下线）。  
若 ECS 免费试用仅约 7 天：恳请支持在预赛关键期 **续期试用或转为按量短周期**，确保公网 Demo URL 在评委验收窗口内持续可用。

### 六、安全与合规承诺

1. 不在申请材料、聊天、代码仓库中明文粘贴真实密码或 API Key。  
2. 公网上线后立即修改默认业务账号密码；评委账号单独管控；口令仅存于本机受控文档或密码管理器。  
3. 尽快启用 **HTTPS（443）**；密钥仅通过环境变量或 gitignore 配置注入，**禁止提交 Git**。  
4. 不在 PPT、视频、对外材料中展示证件、人脸、完整手机号/邮箱等个人信息截图。  
5. 资源仅用于本赛事 Demo 与开发联调，不做与参赛无关的对外经营性滥用；赛事后按要求停机 / 注销敏感配置。

### 七、申请人

- 姓名：[姓名]  
- 工号：[工号]  
- 部门：[部门]  
- 手机：[手机]  
- 邮箱：[邮箱]  

请予审批。谢谢。

申请人：[姓名]  
日期：____年____月____日  

---

## 稿件 B：拆分版（分单走流程）

### B-1 仅申请天翼云试用 ECS

**主题：** 申请天翼云试用 ECS —— 2026「息壤杯」参赛项目「成军台」公网 Demo 主机

尊敬的审批领导 / 相关同事：

为参加 **2026「息壤杯」全国人工智能 OPC 创新大赛**（赛道：**惠民产品创新 · AI+自选开放场景**），需一台可公网访问的天翼云 ECS 部署 Demo，特申请如下。

**项目简介（一句话）：** 成军台（OPC OS on 息壤）——输入目标即可调度 AI 员工矩阵，完成调研、内容、问数、运营与复盘并导出周报。  

补充：仓库 https://github.com/yigenfeng0707-netizen/chengjuntai-opc-xirang ；本地 Demo（`8090`）与评委 60 秒路径、真实标讯约 382 条、过渡模型联调均已就绪；缺公网主机以填写赛事「Demo 地址」。

**用途：** 部署 Nginx + 应用服务，供评委远程访问公网 Demo（登录、看板、样例战役、Word 导出、问数展示）。模型 Token 另单申请，可与本机并行推进。

**规格建议：**

- 4 vCPU / 8 GB / 系统盘 60 GB / 带宽 3 Mbps / Ubuntu 22.04（对齐试用默认档）  
- 安全组放行：**80、443**（公网）；**22**（SSH，建议限源）  
- 应用端口 **8090** 仅本机经 Nginx 反代，不对公网直开  
- 参考：https://www.ctyun.cn/act/trial/central  

**使用期限：** 覆盖预赛至复赛/决赛或赛事结束 + 7～14 天缓冲；试用到期前申请续期或短周期按量，保证验收窗口可用。

**安全承诺：** 不明文传播 root/业务密码；公网改密；启用 HTTPS；密钥与密码不入代码库；仅用于赛事 Demo。

**申请人：** [姓名] / [工号] / [部门] / [手机] / [邮箱]  

请予审批。谢谢。

---

### B-2 仅申请息壤 / 星辰 TokenHub Token

**主题：** 申请息壤 / 星辰 TokenHub Token —— 2026「息壤杯」参赛项目「成军台」主模型推理

尊敬的审批领导 / 相关同事（或赛事对接同事）：

为参加 **2026「息壤杯」全国人工智能 OPC 创新大赛**（赛道：**惠民产品创新 · AI+自选开放场景**），需将竞赛正式主模型切换为 **息壤 / 星辰 TokenHub**，特申请 Token（API Key）及对接参数。

**项目简介（一句话）：** 成军台（OPC OS on 息壤）——目标驱动的一人公司 AI 作战系统（多 Agent + 人审 + Word 周报）。  

补充：仓库 https://github.com/yigenfeng0707-netizen/chengjuntai-opc-xirang ；工程已按双轨对接（primary 占位 + interim 过渡）；本地评委 60 秒路径与真实标讯约 382 条已就绪；公网 Demo 走天翼云 ECS（另单/并行审批）。

**用途：** 预赛公网 Demo 与开发联调的 **primary LLM**（多 Agent 战役编排、内容生成、正式演示口播与模型徽章一致）。到手后仅写入环境变量 `XIRANG_API_KEY`（及可选 `TOKENHUB_API_KEY`），重启服务即可切换，不写入公开仓库。

**需要提供：** API Key、推荐 `api_base`、可用 `model`、额度与有效期。

**调用量级（预估）：** 演示 + 开发联调量级，预计日调用约 **数百～一两千次** 请求（非训练、非大规模爬取）；申请演示开发档额度即可。

**使用期限：** 至复赛/决赛结束或赛事截止日后 7～14 天缓冲。

**安全承诺：** Key 不入库、不入聊天群明文长期存放；不在对外材料中暴露完整密钥；仅用于本作品赛事演示与联调。

**申请人：** [姓名] / [工号] / [部门] / [手机] / [邮箱]  

请予审批 / 协助对接发放。谢谢。

---

## 粘贴提示（给自己）

1. 优先发 **稿件 A**；若云资源与 Token 分属不同审批条线，拆开用 **B-1 / B-2**。  
2. 向主办方要 Token 时，**不要**附身份证、人脸、账单、含隐私的控制台全屏图。  
3. 开通后动作清单见 `docs/USER_ONLY_TODO.md`、`docs/CTYUN_TRIAL.md`（Path A / Path B）。  
4. 公网 URL、真实 Key、改密后的口令只写本机 `ADMIN_ACCESS.local.md`（已 gitignore），勿回填本文件。
